package Clases;

import java.util.ArrayList;

public class Chat {
	
	public void sendMessage(Message msg) {
	//	notifyMessage(msg);
	}
	
	private void notifyMessage(Message msg, Group group) {
		for(int i = 0; i < group.members.size(); i++) {
			// base de datos query
		}
	}
}
